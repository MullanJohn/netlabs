# Base URL of the NetLabs grading API (FastAPI backend).
# Must be PUBLIC_-prefixed to be exposed to the browser (Astro/Vite convention).
PUBLIC_API_BASE_URL=http://localhost:8080

# Postgres used at dev/build time to bake static question JSON (compose db or Neon).
DATABASE_URL=postgresql://temp:temp@localhost:5432/temp
