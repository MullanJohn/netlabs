[project]
name = "backend"
version = "0.1.0"
description = "Backend for the NetLabs platform"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "asyncpg>=0.31.0",
    "fastapi>=0.136.1",
    "pyyaml>=6.0.3",
    "uvicorn>=0.47.0",
]
